package com.hardcoded.db.types;

/**
 * A enum containing the different path types.
 * 
 * @author HardCoded
 * @since v0.2
 */
public enum DatabaseType {
	OBJECTS,
	NODES,
	HARVESTABLES,
	TERRAIN,
	PARTICLES,
	SCRIPTS,
}
